#include "main.h"
/**
 * _strlen - main function
 * @str: string
 * Return: i
 */
 int _strlen(char *str)
{
	int i;
	for (i = 0; str[i] != 0; i++)
		;
	return (i)
}

/**
 * _strlent - main function
 * @str: char
 * Return: i;
 */
int _strlent(const char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
		i
	return (i);
}
